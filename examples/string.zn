use <mem>
use <strings>

i32 main() {

    heap := Allocator{ bytes: 0}


    hello := String::from("hello")

    cap := String::withCapacity(64)

    concat := String::concat("Hello, ", "World!")

    hello.println()

    return 0
}
