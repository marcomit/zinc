use <core::mem>

c :: foreign {
    printf :: u0 (*char, ...)
}

*char self :: for {
    #[override(+)]
    pub sum :: *char (*char other)[Allocator] {
        return self
    }
}

main :: i32 () {
    with _ := Allocator::heap() {
        c::printf("hello" + " world!" + "\n")
    }
    return 0
}
