foreign c {
    u0 printf(*char, ...)
}

i32 sum(i32 a)(i32 b) -> a + b

i32 identity(i32 val) -> val

i32 main() {

    with _ := 10 {
        c::printf("sum %d\n", sum(10))
    }
    with _ := 20 {
        c::printf("sum %d\n", sum(10))
    }

    return 0
}
