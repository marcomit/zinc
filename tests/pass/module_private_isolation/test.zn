use mod2

foreign printf :: u0 (*char, ...)

// Private function with the SAME name as one in mod2. Each file's test
// must stay independent after the per-module LLVM modules are merged.
test :: u0 () {
    printf("mod1\n")
}

main :: i32 () {
    test()
    mod2()
    return 0
}
