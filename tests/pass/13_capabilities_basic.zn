use <core::mem>

c :: foreign {
    printf :: u0 (*char, ...)
}

fill :: i32 (size: u64)[alloc: Allocator] {
    _ := alloc.alloc(size)
    return 42
}

main :: i32 () {
    with _ := Allocator::heap() {
        result: i32 = fill(64)
        c::printf("%d\n", result)
    }
    return 0
}
