use <core::mem>

c :: foreign {
    printf :: u0 (*char, ...)
}

fill :: i32 (u64 size)[Allocator alloc] {
    _ := alloc.alloc(size)
    return 42
}

main :: i32 () {
    with _ := Allocator::heap() {
        i32 result = fill(64)
        c::printf("%d\n", result)
    }
    return 0
}
