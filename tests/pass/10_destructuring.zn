foreign u0 printf(*char, ...)

struct Vec {
    f32 x
    f32 y
}

for Vec {
    Vec new() {
        return Vec{ x: 10, y: 20 }
    }
}

(f32, f32) flatTuple() {
    return (1 as f32, 2 as f32)
}

(*char, i32) labeledTuple() {
    return ("hello", 42)
}

((*char, Vec), i32) nested() {
    return (("ciao", Vec::new()), 99)
}

i32 main() {
    (a, b) := flatTuple()
    printf("%g %g\n", a, b)

    (name, num) := labeledTuple()
    printf("%s %d\n", name, num)

    {x, y} := Vec::new()
    printf("%g %g\n", x, y)

    {x: renamed, y: ry} := Vec::new()
    printf("%g %g\n", renamed, ry)

    ((first, {x: overridden, y: y2}), second) := nested()
    printf("%s %g %g %d\n", first, overridden, y2, second)

    return 0
}
