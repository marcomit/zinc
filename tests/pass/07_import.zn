use <std::core::string>
use <std::mem::gpa>

libc :: foreign {
    printf :: u0 (*char, ...)
}

main :: i32 () {
    gpa := GeneralPurposeAllocator::init()
    with _ := (&gpa).allocator() {
        hello := string::new("Ciao")
        cap := string::with_capacity(64)

        hello2 := string::new("Ciao")
        
        hello.println()

        if hello == hello2 {
            libc::printf("ok\n")
        }
    }

    return 0
}
