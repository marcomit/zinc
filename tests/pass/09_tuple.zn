c :: foreign {
    printf :: u0 (*char, ...)
}

tuple :: (*char, i32)() -> ("john", 40)

main :: i32 () {

    val := tuple()

    (name, age) := tuple()
    
    c::printf("%s: %d\n", name, age)

    return 0
}
