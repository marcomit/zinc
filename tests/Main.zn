// Test: Semantic Analysis
// use "tests/Data.zn"
// use "tests/MathUtil.zn"

type Arru8 = [u8]
type ArrPou8eru8 = [*u8]

struct Vec2 {
	u8 x
	u8 y
}

type Vector = Vec2

void Main() {
    u8 x = 10
    i8 y = -20
    
    // 1. Valid Assignment
    x = y + 5 + c
    
    // 2. L-Value Error (zsem should catch this)
    // Grammar allows it: expression = expression
    // Semantic analyzer should say: "Cannot assign to a constant"
    100 = x 

		// x[3]
    // 3. Pou8er & Unary Check
    // *u8 ptr = &x
    // *ptr = 50 
    
    // 4. Visibility Check (assuming MathUtils is linked)
    // f32 result = Add(1.0, 2.0)            // OK: Public
    f32 bad = u8ernal_square(5.0)       // ERROR: Private in MathUtils.zn
    
    // 5. Loop Check
		while true {
        if x == 5 {
            return
        }
    }
}
 
