// xfail: function parameters are not resolved by the semantic analyser —
// genType(params[i]->resolved) returns void, failing LLVM module verification.
foreign u0 printf(*char)

u0 print_sum(i32 a, i32 b) {
    printf("sum ok\n")
    return
}

u32 main() {
    print_sum(3, 4)
    return 0
}
