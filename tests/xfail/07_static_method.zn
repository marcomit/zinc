// xfail: static method syntax (Type:method) is not yet implemented in the parser —
// the parser expects '(' immediately after the function name, not ':'.
foreign u0 printf(*char)

struct Vec {
    i32 len
}

u0 Vec:new() {
    printf("Vec:new\n")
    return
}

u32 main() {
    Vec:new()
    return 0
}
